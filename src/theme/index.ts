
import { COLORS } from "./colors";
import commonStyles, { hitSlopProp } from "./commonStyles";
import { scale, verticalScale, moderateScale, height, width,moderateScaleVertical } from "./responsiveSize";
import { FONTS } from "./fonts";



export default {
    scale,
    verticalScale,
    moderateScale,
    moderateScaleVertical,
    height,
    width,
    FONTS,
    commonStyles,
    COLORS,
    hitSlopProp
};
